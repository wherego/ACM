#include <iostream>
using namespace std;

int main()
{
	int n;
	cin>>n;
    int a[n];
    int k=0;
    while(n--)
    {
    	int    m, i, j;
    	    c>>a[k++]
    	    for ( i = 0; i < 10; i++ )
    	        if ( m < a[i] )
    	            break;
    	    {
    	        for ( j = 9; j >= i; j-- )
    	            a[j + 1] = a[j];
    	    }
    	    a[i] = m;
    	}

    }

return 0;
}
